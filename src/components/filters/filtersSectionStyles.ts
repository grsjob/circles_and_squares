import styled from "styled-components";

export const StyledFilterSection = styled.section`
  position: absolute;
  height: 1000px;
  left: 0;
  top: 100px;
`;

export const StyledFiltersList = styled.ul`
  margin: 0;
  padding: 0;
  list-style: none;
  background: rgba(217, 217, 217, 0.9);
`;

export const StyledElementFiltersList = styled.ul`
  margin-bottom: 50px;
  padding: 0;
`;

export const StyledInputNumber = styled.input`
  width: 70px;
`;
